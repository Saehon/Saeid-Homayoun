# Benchmark Results

CI writes machine-readable benchmark outputs here at runtime and uploads them as short-retention GitHub Actions artifacts.

The current fixtures are synthetic smoke tests only. They validate the evaluation pipeline; they are **not empirical research findings** and must not be cited as model performance.

Future research runs should include dataset provenance, license, sample period, split design, model/version, prompt/configuration, metrics, uncertainty/error analysis, and human-review status.

CI validation trigger: 2026-09-26. This line has no effect on benchmark logic.
